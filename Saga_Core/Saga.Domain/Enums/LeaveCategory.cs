namespace Saga.Domain.Enums
{
    public enum LeaveCategory
    {
        [Display(Name = "Annual Leave")]
        AnnualLeave = 1,
        [Display(Name = "Pass Leave")]
        PassLeave = 2,
        [Display(Name = "Bonus Leave")]
        BonusLeave = 3,
    }
}
